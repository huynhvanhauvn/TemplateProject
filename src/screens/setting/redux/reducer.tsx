import {SettingState} from '../constants';

const INIT_STATE = {};

const SettingReducer = (state: SettingState = INIT_STATE, action: any) => {
  switch (action.type) {
    default:
      return state;
  }
};

export default SettingReducer;
